import { Form } from "@/widgets/tanstack-form/tanstack-form";

export default async function TanstackFormPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-between p-24">
      <Form />
    </div>
  );
}
