import {DocxNext} from "@/widgets/docx/docx-next";

export default async function FormPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center p-24">
      <DocxNext />
    </div>
  );
}
